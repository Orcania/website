import './bg.scss';

const Background = () => {
    return (
        <div className="bg">
            <section className="hero is-fullheight bg-img">
                <div className="hero-body" />
            </section>
        </div>
    );
};

export default Background;
